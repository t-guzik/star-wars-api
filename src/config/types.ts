/**
 * Used for environment variables validation (all values are strings)
 */
export enum BooleanAsString {
  True = 'true',
  False = 'false',
}

export enum Environment {
  Development = 'development',
  Production = 'production',
  Test = 'test',
  Local = 'local',
}
